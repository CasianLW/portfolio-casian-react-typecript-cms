/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.artic.edu', 'images.unsplash.com', 'res.cloudinary.com'],
  },
}

module.exports = nextConfig
